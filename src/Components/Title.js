import React from 'react'

const title = (props) => {
    return (
        <div className = 'section-title'>
            <h4>{props.title}</h4>
            <div></div>
        </div>
    )
}
export default title;
